if field.label
  | !{field.label.render()}

each choice in field.fields
  input&attributes(choice.attr())= choice.text
  if choice.label
    | !{choice.label.render()}

if field.error && field.error.message
  span.form_error= error.message
